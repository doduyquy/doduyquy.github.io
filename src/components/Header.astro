<header class="site-header sticky top-0 z-50 border-b border-[var(--color-line)] backdrop-blur-xl">
  <div class="page-shell flex min-h-16 items-center justify-between gap-3 py-2">
    <a
      href="/"
      class="link-focus grid size-10 shrink-0 place-items-center rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] text-[var(--color-blue-deep)] transition hover:-translate-y-0.5 hover:border-[var(--color-blue)]"
      aria-label="Home"
      title="Home"
    >
      <svg viewBox="0 0 24 24" class="size-5" aria-hidden="true">
        <path d="M3 10.7 12 3l9 7.7" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
        <path d="M6.5 9.8V21h11V9.8" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
        <path d="M10 21v-6h4v6" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
    </a>

    <label class="hidden min-w-36 max-w-56 flex-1 items-center gap-2 rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] px-4 py-2 text-[var(--color-muted)] md:flex">
      <svg viewBox="0 0 24 24" class="size-4 shrink-0" aria-hidden="true">
        <path d="m21 21-4.35-4.35" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" />
        <circle cx="11" cy="11" r="6.5" fill="none" stroke="currentColor" stroke-width="1.8" />
      </svg>
      <input
        type="search"
        placeholder="Search"
        class="w-full bg-transparent text-sm text-[var(--color-text)] outline-none placeholder:text-[var(--color-muted)]"
      />
    </label>

    <a href="/" class="handwriting link-focus min-w-fit px-2 text-center text-3xl font-semibold text-[var(--color-text)] sm:text-4xl">
      hi, i'm nii
    </a>

    <nav class="flex items-center justify-end gap-2 sm:gap-3" aria-label="Main navigation">
      <button
        id="like-button"
        class="link-focus hidden h-10 items-center gap-1 rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] px-3 text-sm text-[var(--color-blue-deep)] transition hover:border-[var(--color-blue)] sm:flex"
        type="button"
        aria-label="Like"
      >
        <svg viewBox="0 0 24 24" class="size-4" aria-hidden="true">
          <path d="M20.4 5.8a5 5 0 0 0-7.1 0L12 7.1l-1.3-1.3a5 5 0 1 0-7.1 7.1L12 21.3l8.4-8.4a5 5 0 0 0 0-7.1Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round" />
        </svg>
        <span id="like-count">22</span>
      </button>

      <a
        href="/posts"
        class="link-focus rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] px-3 py-2 text-sm text-[var(--color-text)] transition hover:border-[var(--color-blue)] hover:text-[var(--color-blue-deep)]"
      >
        Posts
      </a>

      <button
        class="link-focus hidden rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] px-3 py-2 text-sm text-[var(--color-text)] transition hover:border-[var(--color-blue)] hover:text-[var(--color-blue-deep)] md:inline-flex"
        type="button"
      >
        vi/en
      </button>

      <button
        id="theme-toggle"
        class="link-focus grid size-10 place-items-center rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] text-[var(--color-blue-deep)] transition hover:border-[var(--color-blue)]"
        type="button"
        aria-label="Toggle dark mode"
        title="Toggle dark mode"
      >
        <svg data-theme-icon="sun" viewBox="0 0 24 24" class="size-5 hidden" aria-hidden="true">
          <circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" stroke-width="1.8" />
          <path d="M12 2v2.2M12 19.8V22M4.9 4.9l1.6 1.6M17.5 17.5l1.6 1.6M2 12h2.2M19.8 12H22M4.9 19.1l1.6-1.6M17.5 6.5l1.6-1.6" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" />
        </svg>
        <svg data-theme-icon="moon" viewBox="0 0 24 24" class="size-5" aria-hidden="true">
          <path d="M20.5 15.4A8.5 8.5 0 0 1 8.6 3.5a8.7 8.7 0 1 0 11.9 11.9Z" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
        </svg>
      </button>
    </nav>
  </div>
</header>

<script>
  const themeToggle = document.querySelector<HTMLButtonElement>("#theme-toggle");
  const sunIcon = document.querySelector<SVGElement>('[data-theme-icon="sun"]');
  const moonIcon = document.querySelector<SVGElement>('[data-theme-icon="moon"]');
  const likeButton = document.querySelector<HTMLButtonElement>("#like-button");
  const likeCount = document.querySelector<HTMLElement>("#like-count");

  function syncThemeIcon() {
    const isDark = document.documentElement.classList.contains("dark");
    sunIcon?.classList.toggle("hidden", !isDark);
    moonIcon?.classList.toggle("hidden", isDark);
  }

  themeToggle?.addEventListener("click", () => {
    const isDark = document.documentElement.classList.toggle("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
    syncThemeIcon();
  });

  syncThemeIcon();

  if (likeButton && likeCount) {
    const savedLikes = Number(localStorage.getItem("nii-likes") ?? likeCount.textContent ?? 22);
    likeCount.textContent = String(savedLikes);

    likeButton.addEventListener("click", () => {
      const nextLikes = Number(likeCount.textContent ?? 0) + 1;
      likeCount.textContent = String(nextLikes);
      localStorage.setItem("nii-likes", String(nextLikes));
    });
  }
</script>
